This is an example of a tool that uses my 'libip' (https://github.com/adarqui/libip) repo. If our linux bins were built more modular and supported json objects, we could have much more fun.

LEFTOFF: parsing the obj_t in monitor_handler()
